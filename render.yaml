services:
  - type: web
    name: ctf-game
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --workers 1 --timeout 120 diagnostic:application
    plan: free
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: false
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: ctf-database
          property: connectionString
      - key: MAIL_SUPPRESS_SEND
        value: true
    healthCheckPath: /

databases:
  - name: ctf-database
    databaseName: ctfdb
    user: ctfuser
    plan: free
